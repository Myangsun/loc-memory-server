name: loc-memory-server
version: 1.0.0
description: Local memory MCP server with knowledge graph storage and location extraction
runtime: node
buildCommand: npm install && npm run build
startCommand: node dist/index.js
